pipelines:
  - name: lang_test
    steps:
      - name: u18java8
        type: Bash
        configuration:
          nodePool: u18dynamic
          runtime: 
            type: image
            image: 
              auto:
                language: java
                versions:
                  - "14"
        execution:
          onExecute:
            - printenv
