resources:
  - name:       propertyBag
    type:       PropertyBag
    configuration:
      test: test
pipelines:
  - name: Pipelines_matrixtest
    steps:
      - name: PreMatrix_step
        type: PreMatrix
        configuration:
          inputResources:
            - name: propertyBag      
        execution:
          onStart:
            - pushd $res_propertyBag_resourcePath
            
      - name: Matrix_test
        type: Matrix
        stepMode: Bash
        configuration:
          inputSteps: 
            - name: PreMatrix_step  
        stepletMultipliers:
          environmentVariables:
            - delay: 1
                
        execution:
          onExecute:
            - echo "I am on matrix step"
            - printenv
#             - |
#               [ $fail == 'false' ] 
