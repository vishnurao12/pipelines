
pipelines:
  - name: sample
    steps:
      - name: testBash
        type: Bash
        configuration:
          nodePool: rhel7
          runtime: # optional            
            type: image
            image: # should be present if type is image
              auto:
                language: java
                versions:
                  - "8"
        
        execution:
          onExecute:
            - echo "hello!"
