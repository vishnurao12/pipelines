
pipelines:
  - name: sample
    steps:
      - name: testBash
        type: Bash
        configuration:
          nodePool: u16dynamic
        execution:
          onExecute:
            - echo "hello!"
