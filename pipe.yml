
pipelines:
  - name: sample_feature/branch
    steps:
      - name: testBash_feature/branch
        type: Bash
#         configuration:
#           nodePool: rhel7
#           runtime: # optional            
#             type: image
#             image: # should be present if type is image
#               auto:
#                 language: java
#                 versions:
#                   - "8"
        
        execution:
          onExecute:
            - echo "hello!"
