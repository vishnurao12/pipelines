
pipelines:
  - name: pipeline_win_test
    steps:
      - name: Southeast1_test
        type: PowerShell
        configuration:
          nodePool: u18static
        execution:
          onExecute:
            - write-output  "executing step..."
