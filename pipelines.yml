resources:
  - name: repo_git
    type: GitRepo
    configuration:
      path: vishnurao12/pipelines
      gitProvider: git_v
      
      buildOn:
        commit: false
        pullRequestCreate: true
        pullRequestClose: true
  - name: repo_git_commit
    type: GitRepo
    configuration:
      path: vishnurao12/pipelines
      gitProvider: git_v
      buildOn:
        commit: true
        
  - name: repo_git_false
    type: GitRepo
    configuration:
      path: vishnurao12/pipelines
      gitProvider: git_v
      buildOn:
        pullRequestCreate: false
        
  - name: repo_git_github_true
    type: GitRepo
    configuration:
      path: vishnurao12/pipelines
      gitProvider: git_v
      buildOn:
        pullRequestCreate: true
  - name: repo_git_github_false
    type: GitRepo
    configuration:
      path: vishnurao12/pipelines
      gitProvider: git_v
      buildOn:
        pullRequestClose: true
pipelines:
  - name: test
    steps:
      - name: test
        type: Bash
        configuration:
          inputResources:
            - name: repo_git
              trigger: true
        execution:
          onExecute:
            - printenv  
  - name: test1
    steps:
      - name: test1
        type: Bash
        configuration:
          inputResources:
            - name: repo_git_commit
              trigger: true
        execution:
          onExecute:
            - printenv
  - name: test2
    steps:
      - name: test2
        type: Bash
        configuration:
          inputResources:
            - name: repo_git_false
              trigger: true
        execution:
          onExecute:
            - printenv
  - name: test3
    steps:
      - name: test3
        type: Bash
        configuration:
          inputResources:
            - name: repo_git_github_false
              trigger: true
        execution:
          onExecute:
            - printenv
  - name: test4
    steps:
      - name: test4
        type: Bash
        configuration:
          inputResources:
            - name: repo_git_github_true
              trigger: true
        execution:
          onExecute:
            - printenv


      
