
pipelines:
  - name: pipeline_win_test
    steps:
      - name: Southeast1_test
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
            - write-output  "executing step..."
