resources:
 - name: resource_pipelines124
   type: GitRepo
   configuration:
     gitProvider: git_v
     path: vishnurao12/pipelines
     files: 
       include: ^README.md$
     branches:
       include: ^{{gitBranch}}$
     buildOn:
       pullRequestCreate: true
     cancelPendingRunsOn:
       pullRequestUpdate: true

pipelines:
  - name: testing_123
    steps:
      - name: myStep112
        type: Bash
        configuration:
         inputResources: 
           - name: resource_pipelines124
         integrations:
           - name: git_v
        execution:
          onExecute:
            - printenv
            - pushd $res_resource_pipelines124_resourcePath
            - ls
            - echo "hi"
            - sleep 5m
          onSuccess:
            - pushd $res_resource_pipelines124_resourcePath
            - ls




      
