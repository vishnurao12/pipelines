pipelines:
  - name: extensionstest
    steps:
      - name: step100
        type: sample
        syntaxVersion: 1.0.0
        configuration:              
          foo: bar
        execution:
          onStart:
            - echo "Preparing for work..."
            - printenv