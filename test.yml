pipelines:
  - name: testPipeline
    steps:
      - name: testStepBash
        type: Bash
        configuration:
          nodePool: dynamic_gcp
        execution:
          onExecute:
            - echo "hello"
