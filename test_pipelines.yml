pipelines:
  - name: test_DynamicPool
    configuration:
      nodePool: gcp_dynamic
    steps:
      - name: testBashStep
        type: Bash       
        execution:
          onStart:
            - echo "executing onStart"
          onExecute:
            - sleep 20s
